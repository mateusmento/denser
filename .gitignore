# Dependencies
node_modules
.pnpm-store
dist
*.local
storybook-static

# Env / secrets
.env
.env.*
!.env.example

# Logs / OS
*.log
.DS_Store

# IDE
.idea
.vscode

# Drizzle / generated (keep _journal.json — migrator won't run SQL files missing from it)
packages/api/drizzle/meta/*
!packages/api/drizzle/meta/_journal.json

# Theme Lab → live app bridge (dev)
packages/app/public/theme-dev-override.json


# TS incremental
*.tsbuildinfo

# Playwright
packages/design-system/test-results
packages/design-system/playwright-report
packages/design-system/blob-report

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as ThemeSwitcherStories from "./ThemeSwitcher.stories.vue";

<Meta of={ThemeSwitcherStories} />

# ThemeSwitcher

Light / dark / system control. Spec: `docs/ui-surfaces/theme.md`. Not mounted on Conversation or Document.

<Canvas of={ThemeSwitcherStories.Default} />

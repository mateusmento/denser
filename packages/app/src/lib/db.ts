/**
 * TanStack DB collections live here once domain entities exist.
 * Transport stays on TanStack Query; this module is the canonical replica slot.
 */
export const denserDb = {
  ready: true as const,
};

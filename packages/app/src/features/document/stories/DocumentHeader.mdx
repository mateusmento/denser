import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as DocumentHeaderStories from "./DocumentHeader.stories.vue";

<Meta of={DocumentHeaderStories} />

# DocumentHeader

Quiet identity chrome for the document canvas (`h-surface-header`). Spec: `docs/ui-surfaces/document.md`.

<Canvas of={DocumentHeaderStories.Default} />

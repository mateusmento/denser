import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as TitleEditorStories from "./TitleEditor.stories.vue";

<Meta of={TitleEditorStories} />

# TitleEditor

In-flow document title. Spec: `docs/ui-surfaces/document.md`.

<Canvas of={TitleEditorStories.Default} />

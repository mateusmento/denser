import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as ChannelHeaderStories from "./ChannelHeader.stories.vue";

<Meta of={ChannelHeaderStories} />

# ChannelHeader

Quiet identity chrome for the channel canvas (`h-surface-header`). Spec: `docs/ui-surfaces/conversation.md`.

<Canvas of={ChannelHeaderStories.Default} />

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as MessageContextMenuStories from "./MessageContextMenu.stories.vue";

<Meta of={MessageContextMenuStories} />

# MessageContextMenu

Right-click menu for a channel message: react, reply in thread, copy / copy link, bookmark, forward, quote, edit, delete. Default slot is the trigger.

<Canvas of={MessageContextMenuStories.Default} />

import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as SchedulePopoverStories from "./SchedulePopover.stories.vue";

<Meta of={SchedulePopoverStories} />

# SchedulePopover

Preset picker for scheduling a message. Spec: `docs/ui-surfaces/conversation.md`.

<Canvas of={SchedulePopoverStories.Open} />

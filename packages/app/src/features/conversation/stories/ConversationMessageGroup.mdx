import * as ConversationMessageGroupStories from "./ConversationMessageGroup.stories.vue";

<Meta of={ConversationMessageGroupStories} />

# ConversationMessageGroup

Same-author cluster: avatar and header once, slotted `ConversationMessage`s.

<Canvas of={ConversationMessageGroupStories.Cluster} />
